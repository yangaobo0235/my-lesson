# 安全策略

## 支持范围

安全修复应用于 `main` 分支的最新版本。历史提交和本地开发快照不单独维护安全更新。

## 报告安全问题

请不要在公开 Issue 中披露密码、令牌、个人数据或可利用的漏洞。

优先使用 GitHub 仓库的 Private Security Advisory 报告问题，并提供：

- 受影响的模块和版本；
- 可复现步骤；
- 潜在影响；
- 已知的缓解方案。

上传日志、截图或请求数据前，必须移除密码、Cookie、Authorization、API Key、个人信息和业务敏感数据。

## 仓库密钥规则

- 真实配置只能保存在 `.env`、`deploy/env/*.env` 或平台密钥系统中。
- Git 只提交 `.env.example` 和 `*.env.example`，其中只能使用占位值。
- 不得提交数据库密码、模型 API Key、JWT 密钥、内部令牌、支付私钥或云服务凭据。
- 凭据一旦进入提交历史或公开渠道，应立即轮换；仅删除当前文件不足以消除泄漏风险。
- 生产服务使用独立的最小权限数据库、Redis、MinIO 和消息系统账号。

## Agent 安全边界

- Python Agent 不直接写入 Java 业务数据库。
- Java 工具必须同时校验内部服务令牌和 Gateway 委托身份。
- 用户身份只能来自已验证的安全上下文，不能从模型或工具参数中信任 `userId`。
- Prompt、检索内容和模型输出均视为不可信输入，不能绕过权限、确认和业务规则。
